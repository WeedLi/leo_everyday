<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <style name="HorpaddingView">
        <item name="android:layout_height">@dimen/padding1</item>
        <item name="android:layout_width">match_parent</item>
    </style>

    <style name="VerpaddingView">
        <item name="android:layout_height">10dp</item>
        <item name="android:layout_width">@dimen/padding0</item>
    </style>

    <style name="HorpaddingLine">
        <item name="android:layout_height">0.8dp</item>
        <item name="android:background">@color/line_color</item>
        <item name="android:layout_marginLeft">@dimen/padding1</item>
        <item name="android:layout_width">match_parent</item>
    </style>


</resources>
